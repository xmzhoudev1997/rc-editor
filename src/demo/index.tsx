import React, { type FC } from 'react';
import { RCEditor } from '@xmzhou/rc-editor';
import './index.less';

const Foo: FC = () => {
    return <RCEditor
        className="demo-editor"
    />;
};

export default Foo;
